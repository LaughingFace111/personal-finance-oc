# 记账2代码修复任务

## 一、代码基线
请以 **commit 865d8ab** 作为原版本基线来理解本次需求和偏差来源。

## 二、原始需求简述

### 1. 添加交易入口应拆成 3 个页面/入口
- A. 收入/支出页面：收入和支出放在同一个页面，页面内部用切换方式区分
- B. 转账页面：单独一个页面
- C. 其他页面：分期付款、借出、借入等低频交易

### 2. FAB 逻辑必须同步修改
点击 FAB 后，弹出 3 个悬浮按钮入口：收入/支出、转账、其他。每个入口有图标+文字提示。

### 3. 不能只改前端样式，必须同步实现后端逻辑

## 三、当前问题

当前仍然只是一个简陋的添加交易页面，没有拆分成3个页面。FAB功能也没有正确改造。

## 四、任务

### 第一步：问题分析
基于 commit 865d8ab 原版本和当前代码，分析：
1. 当前代码为什么没有实现成3个页面入口？
2. FAB为什么没有被正确改造成3个入口？
3. 根因是什么？

### 第二步：代码修改

#### A. 添加交易入口结构
1. 收入/支出页面：收入和支出共用一个页面，页面内部切换
2. 转账页面：独立页面
3. 其他页面：分期付款、借出、借入

#### B. FAB 入口逻辑
点击 FAB 后弹出3个入口按钮，每个有图标+文字，点击后跳到对应页面

#### C. 不要只做页面壳子
必须打通：路由、表单、提交逻辑、后端接口调用、成功反馈

### 五、输出要求
1. 问题分析
2. 修改方案
3. 实际改动
4. 验证结果
5. 总结
